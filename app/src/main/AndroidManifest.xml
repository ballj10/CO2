<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.co2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".roll"></activity>
        <activity
            android:name=".CharacterInfo"
            android:label="@string/title_activity_character_info" />

        <activity
            android:name=".SpellListActivity"
            android:label="@string/title_spell_list"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".SpellDetailActivity"
            android:label="@string/title_spell_detail"
            android:parentActivityName=".SpellListActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.co2.SpellListActivity" />
        </activity>

        <activity android:name=".CharacterSelection" />
        <activity android:name=".HomePage" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>